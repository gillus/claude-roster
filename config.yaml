instances:
  # Remote VMs — accessed via SSH
  - name: gcp-dev-1
    type: remote
    host: 34.xxx.xxx.xxx
    user: ubuntu
    key: ~/.ssh/gcp-dev.pem
    provider: gcp
  # Local instances — runs on your machine
  - name: CoCu
    type: local
    working_dir: /home/gillus/CoCu
    provider: local

