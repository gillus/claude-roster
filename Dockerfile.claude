FROM node:22-slim

RUN apt-get update && apt-get install -y git openssh-client ncurses-bin && rm -rf /var/lib/apt/lists/*
RUN npm install -g @anthropic-ai/claude-code

ENV TERM=xterm-256color
WORKDIR /workspace
ENTRYPOINT ["claude"]
